#include <stdio.h>
#include <stdlib.h>

int getSum(int n)
{
	return n*(n=1)/2;
}

main()
{
	int x;

	x = 10;

	if(x == 10)
	{
		printf("1111\n");
	}
	else
	{
		printf("2222\n");
	}
	//c언어 에서 if문 전제 조건 0이면 거짓 나머지 숫자는 참이 된다.
	//if문 조건식 판단이 위에서부터 생행되서 비교 순서가중요하다. 동시 패션이라니라 절차지행이라 주의가 필요하다.
	//조건이 겹치는 것도 주의해야 하지만 변수의 범위를 생각해서 놓치는 범위가 없게 해야한다. 


	/*
	while(1)
	{
		printf(" Type a number: ");
		scanf("%d",&x);

		if(x%2 == 0) //(x/2*2 ==x) 순서 함정 나올수 있음
		{
			printf("even number\n");
		}
		else
		{
			printf("odd number\n");
		}
	}
	*/


}
